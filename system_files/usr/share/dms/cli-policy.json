{
  "policy_version": 1,
  "blocked_commands": [
    "greeter install",
    "greeter enable",
    "greeter uninstall"
  ],
  "message": "greetd + tuigreet are baked into the ublue-niri image (system_files/etc/greetd); greeter changes happen via image rebuilds. User-level `dms setup` config deploys are allowed."
}
